// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'theme.dart';

// **************************************************************************
// CopyWithGenerator
// **************************************************************************

abstract class _$SpacingThemeCWProxy {
  SpacingTheme spacing(Spacing spacing);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `SpacingTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// SpacingTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  SpacingTheme call({
    Spacing? spacing,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfSpacingTheme.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfSpacingTheme.copyWith.fieldName(...)`
class _$SpacingThemeCWProxyImpl implements _$SpacingThemeCWProxy {
  final SpacingTheme _value;

  const _$SpacingThemeCWProxyImpl(this._value);

  @override
  SpacingTheme spacing(Spacing spacing) => this(spacing: spacing);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `SpacingTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// SpacingTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  SpacingTheme call({
    Object? spacing = const $CopyWithPlaceholder(),
  }) {
    return SpacingTheme(
      spacing: spacing == const $CopyWithPlaceholder() || spacing == null
          ? _value.spacing
          // ignore: cast_nullable_to_non_nullable
          : spacing as Spacing,
    );
  }
}

extension $SpacingThemeCopyWith on SpacingTheme {
  /// Returns a callable class that can be used as follows: `instanceOfclass SpacingTheme.name.copyWith(...)` or like so:`instanceOfclass SpacingTheme.name.copyWith.fieldName(...)`.
  _$SpacingThemeCWProxy get copyWith => _$SpacingThemeCWProxyImpl(this);
}

abstract class _$ColorsThemeCWProxy {
  ColorsTheme backgroundColor(Color backgroundColor);

  ColorsTheme blackTextColor(Color blackTextColor);

  ColorsTheme hoverColor(Color hoverColor);

  ColorsTheme paneColor(Color paneColor);

  ColorsTheme primaryColor(Color primaryColor);

  ColorsTheme shadowColor(Color shadowColor);

  ColorsTheme whiteTextColor(Color whiteTextColor);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `ColorsTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// ColorsTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  ColorsTheme call({
    Color? backgroundColor,
    Color? blackTextColor,
    Color? hoverColor,
    Color? paneColor,
    Color? primaryColor,
    Color? shadowColor,
    Color? whiteTextColor,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfColorsTheme.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfColorsTheme.copyWith.fieldName(...)`
class _$ColorsThemeCWProxyImpl implements _$ColorsThemeCWProxy {
  final ColorsTheme _value;

  const _$ColorsThemeCWProxyImpl(this._value);

  @override
  ColorsTheme backgroundColor(Color backgroundColor) =>
      this(backgroundColor: backgroundColor);

  @override
  ColorsTheme blackTextColor(Color blackTextColor) =>
      this(blackTextColor: blackTextColor);

  @override
  ColorsTheme hoverColor(Color hoverColor) => this(hoverColor: hoverColor);

  @override
  ColorsTheme paneColor(Color paneColor) => this(paneColor: paneColor);

  @override
  ColorsTheme primaryColor(Color primaryColor) =>
      this(primaryColor: primaryColor);

  @override
  ColorsTheme shadowColor(Color shadowColor) => this(shadowColor: shadowColor);

  @override
  ColorsTheme whiteTextColor(Color whiteTextColor) =>
      this(whiteTextColor: whiteTextColor);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `ColorsTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// ColorsTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  ColorsTheme call({
    Object? backgroundColor = const $CopyWithPlaceholder(),
    Object? blackTextColor = const $CopyWithPlaceholder(),
    Object? hoverColor = const $CopyWithPlaceholder(),
    Object? paneColor = const $CopyWithPlaceholder(),
    Object? primaryColor = const $CopyWithPlaceholder(),
    Object? shadowColor = const $CopyWithPlaceholder(),
    Object? whiteTextColor = const $CopyWithPlaceholder(),
  }) {
    return ColorsTheme(
      backgroundColor: backgroundColor == const $CopyWithPlaceholder() ||
              backgroundColor == null
          ? _value.backgroundColor
          // ignore: cast_nullable_to_non_nullable
          : backgroundColor as Color,
      blackTextColor: blackTextColor == const $CopyWithPlaceholder() ||
              blackTextColor == null
          ? _value.blackTextColor
          // ignore: cast_nullable_to_non_nullable
          : blackTextColor as Color,
      hoverColor:
          hoverColor == const $CopyWithPlaceholder() || hoverColor == null
              ? _value.hoverColor
              // ignore: cast_nullable_to_non_nullable
              : hoverColor as Color,
      paneColor: paneColor == const $CopyWithPlaceholder() || paneColor == null
          ? _value.paneColor
          // ignore: cast_nullable_to_non_nullable
          : paneColor as Color,
      primaryColor:
          primaryColor == const $CopyWithPlaceholder() || primaryColor == null
              ? _value.primaryColor
              // ignore: cast_nullable_to_non_nullable
              : primaryColor as Color,
      shadowColor:
          shadowColor == const $CopyWithPlaceholder() || shadowColor == null
              ? _value.shadowColor
              // ignore: cast_nullable_to_non_nullable
              : shadowColor as Color,
      whiteTextColor: whiteTextColor == const $CopyWithPlaceholder() ||
              whiteTextColor == null
          ? _value.whiteTextColor
          // ignore: cast_nullable_to_non_nullable
          : whiteTextColor as Color,
    );
  }
}

extension $ColorsThemeCopyWith on ColorsTheme {
  /// Returns a callable class that can be used as follows: `instanceOfclass ColorsTheme.name.copyWith(...)` or like so:`instanceOfclass ColorsTheme.name.copyWith.fieldName(...)`.
  _$ColorsThemeCWProxy get copyWith => _$ColorsThemeCWProxyImpl(this);
}

abstract class _$TextThemeCWProxy {
  TextTheme button(TextStyle button);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `TextTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// TextTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  TextTheme call({
    TextStyle? button,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfTextTheme.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfTextTheme.copyWith.fieldName(...)`
class _$TextThemeCWProxyImpl implements _$TextThemeCWProxy {
  final TextTheme _value;

  const _$TextThemeCWProxyImpl(this._value);

  @override
  TextTheme button(TextStyle button) => this(button: button);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `TextTheme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// TextTheme(...).copyWith(id: 12, name: "My name")
  /// ````
  TextTheme call({
    Object? button = const $CopyWithPlaceholder(),
  }) {
    return TextTheme(
      button: button == const $CopyWithPlaceholder() || button == null
          ? _value.button
          // ignore: cast_nullable_to_non_nullable
          : button as TextStyle,
    );
  }
}

extension $TextThemeCopyWith on TextTheme {
  /// Returns a callable class that can be used as follows: `instanceOfclass TextTheme.name.copyWith(...)` or like so:`instanceOfclass TextTheme.name.copyWith.fieldName(...)`.
  _$TextThemeCWProxy get copyWith => _$TextThemeCWProxyImpl(this);
}

abstract class _$ThemeCWProxy {
  Theme colors(ColorsTheme colors);

  Theme spacing(SpacingTheme spacing);

  Theme text(TextTheme text);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `Theme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// Theme(...).copyWith(id: 12, name: "My name")
  /// ````
  Theme call({
    ColorsTheme? colors,
    SpacingTheme? spacing,
    TextTheme? text,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfTheme.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfTheme.copyWith.fieldName(...)`
class _$ThemeCWProxyImpl implements _$ThemeCWProxy {
  final Theme _value;

  const _$ThemeCWProxyImpl(this._value);

  @override
  Theme colors(ColorsTheme colors) => this(colors: colors);

  @override
  Theme spacing(SpacingTheme spacing) => this(spacing: spacing);

  @override
  Theme text(TextTheme text) => this(text: text);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `Theme(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// Theme(...).copyWith(id: 12, name: "My name")
  /// ````
  Theme call({
    Object? colors = const $CopyWithPlaceholder(),
    Object? spacing = const $CopyWithPlaceholder(),
    Object? text = const $CopyWithPlaceholder(),
  }) {
    return Theme(
      colors: colors == const $CopyWithPlaceholder() || colors == null
          ? _value.colors
          // ignore: cast_nullable_to_non_nullable
          : colors as ColorsTheme,
      spacing: spacing == const $CopyWithPlaceholder() || spacing == null
          ? _value.spacing
          // ignore: cast_nullable_to_non_nullable
          : spacing as SpacingTheme,
      text: text == const $CopyWithPlaceholder() || text == null
          ? _value.text
          // ignore: cast_nullable_to_non_nullable
          : text as TextTheme,
    );
  }
}

extension $ThemeCopyWith on Theme {
  /// Returns a callable class that can be used as follows: `instanceOfclass Theme.name.copyWith(...)` or like so:`instanceOfclass Theme.name.copyWith.fieldName(...)`.
  _$ThemeCWProxy get copyWith => _$ThemeCWProxyImpl(this);
}
